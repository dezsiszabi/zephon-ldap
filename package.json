{
  "name": "zephon-ldap",
  "version": "1.0.0",
  "main": "main.js",
  "types": "main.d.ts",
  "description": "OpenLDAP bindings for node",
  "private": false,
  "gypfile": true,
  "scripts": {
    "configure": "node-gyp configure",
    "build": "tsc -p . && node-gyp --debug rebuild"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^4.3.0"
  },
  "engines": {
    "node": ">=8.4.0"
  },
  "author": "Szabolcs Dezsi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.22",
    "typescript": "^4.5.5"
  }
}
